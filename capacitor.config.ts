import type { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig = { appId:'com.risingtech.ciwaan', appName:'Ciwaan', webDir:'dist', server:{ androidScheme:'https' } }
export default config
